// Semantic icon registry — map meaning to glyph so the icon for a concept
// is swapped in one place. Import these instead of lucide glyphs for shared meanings.
export {
  Check as Success,
  TriangleAlert as Warning,
  Download,
  Trash2 as Delete,
  X as Close,
  Loader as Processing,
  ShieldOff as Expired,
} from "lucide-react";
