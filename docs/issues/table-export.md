The table-export is an example of a collection of actions for a table.

Should we pass the actions bar as a prop?

Otherwise we have awkward handling of empty-tables in multiple locations.

---


Is the admin user table using the old expansion pattern?